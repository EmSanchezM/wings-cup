# Foundation - Verify Report

**Verdict**: WARNINGS
**Date**: 2026-05-10
**Verifier**: sdd-verify (sonnet)
**HEAD**: d5d1f8f

---

## Executive Summary

The foundation slice is substantially complete. All 7 tables, 3 migrations, auth pages, server utils, tests, and tooling are in place. pnpm lint, pnpm test:unit (7 tests), pnpm test:nuxt (2 tests) all pass. pnpm build exits 0 after nuxt prepare. 0 CRITICAL, 4 WARNINGS, 5 SUGGESTIONS. Not blocking archive, but W-01/W-02 (missing super-admin RLS policies) should be fixed before slice 2.

---

## Verification Coverage

| Check | Status | Notes |
|-------|--------|-------|
| 1. File existence | passed | All required files present |
| 2. SQL vs design | passed | 7 tables, columns, indexes, REPLICA IDENTITY FULL |
| 3. RLS coverage | warnings | 2 missing super-admin SELECT policies |
| 4. Triggers | passed | 5 functions defined and bound |
| 5. Auth pages | warnings | login.vue missing data.display_name in OTP |
| 6. server/utils/auth.ts | passed | serverSupabaseServiceRole used correctly |
| 7. nuxt.config.ts | passed | All required keys correct |
| 8. vercel.json | passed | crons:[], buildCommand, installCommand |
| 9. .env.example | partial | OS permission denied - verify limitation |
| 10. README.md | passed | 7-step boot block, scripts, gotcha, super-admin SQL |
| 11. pnpm test:unit | passed | 7/7 |
| 12. pnpm test:nuxt | passed | 2/2 |
| 13. pnpm lint | passed | exit 0 |
| 14. pnpm build | warnings | cold build fails; passes after nuxt prepare |
| 15. Apply-progress trace | passed | 3/3 merge hashes confirmed |

---

## Findings

### CRITICAL

None.

---

### WARNINGS

**W-01** 00002_rls.sql - R-SEC-11: rooms SELECT super-admin policy missing. Only rooms_select_member exists. Add rooms_select_super_admin in migration 00004_rls_superadmin.sql before slice 2.

**W-02** 00002_rls.sql - R-SEC-24: predictions SELECT super-admin policy missing. Only pred_select_room_members exists. Add pred_select_super_admin in same migration as W-01.

**W-03** app/pages/auth/login.vue - R-AUTH-13: signInWithOtp missing options.data.display_name. Magic-link users get display_name from email prefix fallback. Acceptable to defer to slice 2 if /join/[code] collects name. Document the decision.

**W-04** Build pipeline - R-DEP-18: Cold pnpm build (no .nuxt/) fails TS2304/TS2353. After nuxt prepare succeeds. Add prepare script to package.json or document in README.

---

### SUGGESTIONS

**S-01** scoring-rules.schema.ts: wrong field not in SQL default; adds wrong:0 on DB roundtrip. Remove or document.

**S-02** Trigger names deviate from spec (accepted per apply-progress). Note in archive only.

**S-03** apply-progress shows PR 3 as OPEN. All 3 PRs are merged (HEAD d5d1f8f). Update before archive.

**S-04** @nuxtjs/supabase emits types path warning at build. Cosmetic. Track upstream.

**S-05** .env.example could not be read (OS permission). Manually verify 4 required vars.

---

## Accepted Deviations (pre-approved)

rooms.status: active/closed vs spec active/archived. audit_log column: target_type vs target_table. Trigger name: on_auth_user_created vs auth_users_create_profile. API name: serverSupabaseServiceRole vs typo in design. Cookie redirect: .pluck() method. Vitest: test.projects in vitest.config.ts (authoritative). shadcn-vue: manual scaffold.

---

## Test Execution Results

| Command | Exit code | Details |
|---------|-----------|---------|
| pnpm lint | 0 | Clean |
| pnpm build cold | 2 | TS auto-import errors |
| pnpm prepare + build | 0 | 820 client + 261 server modules |
| pnpm test:unit | 0 | 7/7 in 1.66s |
| pnpm test:nuxt | 0 | 2/2 in 24s (needs placeholder Supabase env) |
| Service key in .output/ | absent | grep exit 1 |

---

## Apply-Progress Trace

| PR | Expected | Match |
|----|----------|-------|
| PR 1 | 25f0072 | YES |
| PR 2 | bef4e06 | YES |
| PR 3 | d5d1f8f | YES |

---

## SQL vs Design

| Requirement | Status |
|------------|--------|
| 7 tables all columns | PASS |
| predicted scores CHECK 0-15 | PASS |
| invitations.expires_at 7 days | PASS |
| room_members REPLICA IDENTITY FULL | PASS |
| All indexes present | PASS |
| ENABLE RLS all 7 tables | PASS |
| rooms super-admin SELECT | FAIL W-01 |
| predictions super-admin SELECT | FAIL W-02 |
| REVOKE UPDATE(total_points) | PASS |
| 5 trigger functions | PASS |
| calculate_points priority comment | PASS |
| lock_started_predictions RPC | PASS |

---

## Recommendation

WARNINGS - proceed to archive after addressing W-01 and W-02 (missing super-admin RLS). Add migration 00004_rls_superadmin.sql. W-04 is a quick package.json fix. W-03 can be deferred to slice 2 with documented intent.

After W-01/W-02 fixed: sdd-archive foundation

*Generated by sdd-verify (sonnet) - Standard Mode - wings-cup - 2026-05-10*